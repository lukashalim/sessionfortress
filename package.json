{
  "name": "session-fortress",
  "version": "0.1.3",
  "private": true,
  "description": "Save Chrome windows and Tab Groups. Mirror every save to a folder you control.",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/bundle-sw.mjs && node scripts/generate-icons.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/selftest.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.11"
  }
}
